/**
 * 
 */

/**
 * @author Jimmy Chavada
 * Date: OCT 2020
 *
 */
public class ArrayExample1 {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		// declares and creates arrays 
		int age[];// declares the array  
		age = new int[5]; // creates the array for a value of 5 
		
		age [0] = 10; 
		age [1] = 5;
		System.out.println(age[0] + "\n" + age[1] + "\n" + age[2]);
	
		// creates, declares, and initializes arrays 
		String name[] = {"Joanne", "Campos", "James", "Aryan", "John"};
		System.out.println(name[2]);
		
		
	}

}
